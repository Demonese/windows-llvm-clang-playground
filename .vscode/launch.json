{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "main",
            "program": "${workspaceRoot}/build/main/main.exe",
            "args": [],
            "env": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}